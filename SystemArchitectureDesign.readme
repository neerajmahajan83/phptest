1) Lead & Trial Module: Handles the business logic for "1 Free Trial Per Lead" and the 12-hour rescheduling constraint.

2) Scheduling Engine: The "Brain" that cross-references trainer_availability and member_membership_status in real-time before confirming a slot.

3) Laravel Horizon (Redis): This is critical for DLF Thrive. It ensures that if the WhatsApp API is slow or down, the Staff CRM doesn't freeze. It handles retries automatically.

4) Scheduled Tasks (Cron): A heartbeat that runs every minute to find memberships expiring in 7 days or trials starting in 2 hours, pushing them into the notification queue.

5) PostgreSQL: Chosen over MySQL for its superior handling of complex relational data and partial-payment ledgering (using JSONB for audit logs).
